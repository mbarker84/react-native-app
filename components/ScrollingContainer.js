import React, { Component } from 'react'
import { Text, ScrollView } from 'react-native'

class ScrollingContainer extends Component {
	render() {
		return (
			<ScrollView>
				<Text>Scroll this.........</Text>
			</ScrollView>
		)
	}
}

export default ScrollingContainer
